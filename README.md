# AI Debate Arena

Minimal Next.js foundation for an AI debate application.

## Getting started

```bash
npm install
npm run dev
```

Checks: `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`.
Provider setup is intentionally a placeholder: `npm run provider:add`.

See `docs/` for architecture and development notes.
