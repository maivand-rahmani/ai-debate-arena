# Debate engine

The P0 domain exposes provider-agnostic debate phases, typed agents/turns/verdicts,
deterministic progression, bounded prompt history, and Zod-validated judge output.
The active Quick token policy allows 1200 agent output tokens and 1000 judge output tokens.
