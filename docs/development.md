# Development

Use Node.js and npm. Run `npm run dev` for local development.

Before a change is submitted, run `npm run lint`, `npm run typecheck`, `npm test`,
and `npm run build`. Environment values belong in `.env.local`; never commit secrets.
