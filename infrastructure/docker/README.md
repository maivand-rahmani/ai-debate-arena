# infrastructure/docker

Docker sandbox is intentionally deferred to v0.5.

This directory is reserved for the future worker/sandbox images (debate
worker runtime, model-call sandbox). No Dockerfiles, compose files, or
build scripts live here yet — do not add any until the v0.5 sandbox design
lands.
